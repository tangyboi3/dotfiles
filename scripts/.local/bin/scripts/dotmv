#!/bin/sh

curdir=$(pwd)
pkg=$1
filepath=$(realpath $2) 
relpath="${filepath/#${HOME}\//}"
dir=$(dirname $relpath)
dotdir=$HOME/dotfiles/$pkg/$dir

echo "moving: ~/"$relpath
echo
echo "to: ~/dotfiles/"$pkg

mkdir -p $dotdir
mv $filepath $dotdir

cd $HOME/dotfiles
stow $pkg
cd $curdir
