exec {
    gammastep -P -O 4000
    wal -R
    udiskie &
}

exec_always {
    killall yambar; cat ~/.cache/wal/colors-yambar.yml ~/.config/yambar/config.yml > /tmp/yambar.yml; yambar -c /tmp/yambar.yml
    autotiling
}
