gaps inner 8px 
gaps outer 4px 

font pango:IosevkaNerdFont 12 "#ss20"

output * bg $wallpaper fill

default_border none

#client.class	border	background	text		indicator	child_border
client.focused	$color7	$background	$foreground	$color7		$color7

# combine config files for yambar
exec_always cat ~/.cache/wal/yambar.yml ~/.config/yambar/config.yml > /tmp/yambar.yml; killall yambar; yambar -c /tmp/yambar.yml &
